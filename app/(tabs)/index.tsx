// app/(tabs)/index.tsx
import { Text, View } from 'react-native';

export default function TabIndex() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Trang chủ (Tabs)</Text>
    </View>
  );
}